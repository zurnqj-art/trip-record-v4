const KEY="trip_v4";let records=JSON.parse(localStorage.getItem(KEY)||"[]");document.getElementById("date").value=new Date().toISOString().split("T")[0];function save(){localStorage.setItem(KEY,JSON.stringify(records));render()}function addRecord(){const r={date:date.value,customer:customer.value,reason:reason.value,start:start.value,dest:dest.value,distance:+distance.value||0,parking:+parking.value||0,rate:+rate.value||5};if(!r.customer||!r.reason){alert("請填寫客戶與事由");return;}records.push(r);save();}function render(){let h='',s=0;records.forEach((r,i)=>{let t=r.distance*r.rate+r.parking;s+=t;h+=`<tr><td>${r.date}</td><td>${r.customer}</td><td>${r.reason}</td><td>${t}</td><td><button onclick='delRec(${i})'>刪除</button></td></tr>`});tbody.innerHTML=h;total.textContent=s;}function delRec(i){records.splice(i,1);save()}function exportJSON(){const b=new Blob([JSON.stringify(records,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='trip-backup.json';a.click()}function importJSON(e){const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=x=>{records=JSON.parse(x.target.result);save()};r.readAsText(f)}function exportCSV(){let c='日期,客戶,事由
';records.forEach(r=>c+=`${r.date},${r.customer},${r.reason}
`);const b=new Blob(['﻿'+c]);const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='trip.csv';a.click()}render(); if('serviceWorker' in navigator){navigator.serviceWorker.register('service-worker.js');}